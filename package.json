{
  "name": "index-agent-staking",
  "version": "1.0.0",
  "description": "Index Network Agent Staking Smart Contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network monad-testnet",
    "verify": "hardhat verify --network monad-testnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@openzeppelin/contracts": "^4.9.3",
    "hardhat": "^2.19.0",
    "dotenv": "^16.3.1"
  }
}
